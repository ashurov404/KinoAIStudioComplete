export type AIResult={text:string;model:string;raw?:any};
export async function callOpenRouter(apiKey:string,prompt:string,model='openai/gpt-4o-mini'):Promise<AIResult>{if(!apiKey)throw new Error('OPENROUTER_API_KEY_MISSING');const r=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${apiKey}`},body:JSON.stringify({model,messages:[{role:'system',content:'You are the Kino AI Studio screenplay and scene-planning engine. Return concise production-ready text or JSON only.'},{role:'user',content:prompt}],temperature:.65})});if(!r.ok)throw new Error(`OPENROUTER_${r.status}`);const j=await r.json();return{text:j?.choices?.[0]?.message?.content||'',model,raw:j}}
export async function generateFilmPlan(apiKey:string,script:string,model='openai/gpt-4o-mini'){const p=`Convert this Uzbek film idea into valid JSON with keys title,genre,characters[],locations[],scenes[]. Each scene must contain shots[] with duration,camera,lighting,environment,action,dialogue,emotion,characterNames. Do not invent impossible continuity changes. Script:\n${script}`;return callOpenRouter(apiKey,p,model)}
